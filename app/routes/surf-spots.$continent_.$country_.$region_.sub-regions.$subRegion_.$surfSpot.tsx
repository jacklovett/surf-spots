/**
 * Same as `$surfSpot_.session` under sub-regions: URL shape only differs from the
 * non-sub-region spot detail route. Reuse that loader/action/UI.
 */
export { loader, action, default } from './surf-spots.$continent_.$country_.$region_.$surfSpot'
